# SVP-MPV Integration Script

# Enable SVP communication with MPV
run_at_start="svpcode"

# SVP's playback options
svp.enable=true
svp.allow_10bit=true
svp.allow_hwdec=true

# Logging (optional, for debugging)
log.level=debug
log.file=~/.config/SVP/svp-mpv.log

